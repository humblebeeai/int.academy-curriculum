services:
  docs-site:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - REACT_APP_CHAT_LINK=${REACT_APP_CHAT_LINK}
    image: internal-academy-docs:latest
    container_name: internal-academy-docs
    ports:
      - "${PORT:-8000}:80"
    restart: unless-stopped
